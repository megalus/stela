# Example Chalice App

## Instructions
1. Run `uv sync --active --group dev`
2. Go to `examples/chalice/test-stela` folder
3. Run `uv run chalice local --no-autoupdate`
4. Go to `http://localhost:8000/`
